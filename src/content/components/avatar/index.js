import { bordered } from "./bordered";
import { colors } from "./colors";
import { customStyles } from "./customStyles";
import { disabled } from "./disabled";
import { fallbacks } from "./fallbacks";
import { group } from "./group";
import { groupCustomCount } from "./groupCustomCount";
import { groupDisabled } from "./groupDisabled";
import { groupGrid } from "./groupGrid";
import { groupMaxCount } from "./groupMaxCount";
import { groupTotalCount } from "./groupTotalCount";
import { radius } from "./radius";
import { sizes } from "./sizes";
import { usage } from "./usage";

export const avatarContent = {
	usage: usage,
	sizes: sizes,
	disabled: disabled,
	bordered: bordered,
	radius: radius,
	colors: colors,
	fallbacks: fallbacks,
	group: group,
	groupDisabled: groupDisabled,
	groupMaxCount: groupMaxCount,
	groupTotalCount: groupTotalCount,
	groupCustomCount: groupCustomCount,
	groupGrid: groupGrid,
	customStyles: customStyles,
};
