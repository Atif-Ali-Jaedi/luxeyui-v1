import { colors } from "./colors";
import { controlledVisibility } from "./controlledVisibility";
import { customIcon } from "./customIcon";
import { customStyles } from "./customStyles";
import { radius } from "./radius";
import { usage } from "./usage";
import { variants } from "./variants";
import { withAction } from "./withAction";
import { withOutIcon } from "./withoutIcon";
import { withOutIconWrapper } from "./withoutIconWrapper";

export const alertContent = {
	usage: usage,
	colors: colors,
	variants: variants,
	radius: radius,
	customIcon: customIcon,
	withOutIcon: withOutIcon,
	withOutIconWrapper: withOutIconWrapper,
	withAction: withAction,
	controlledVisibility: controlledVisibility,
	customStyles: customStyles,
};
